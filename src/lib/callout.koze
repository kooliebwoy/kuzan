<script>
  import { props } from 'koze:component';
  const { color = 'var(--kui-fg-muted)', class: className = '' } = props;
</script>

<div class="kui-callout {className}" style="--callout-color: {color}">
  <div class="kui-callout__body">
    <slot></slot>
  </div>
</div>

<style>
  .kui-callout {
    padding: 1rem 1.25rem;
    border-radius: var(--kui-radius-lg);
    border: 1px solid color-mix(in srgb, var(--callout-color) 30%, var(--kui-border));
    background: color-mix(in srgb, var(--callout-color) 5%, var(--kui-bg));
    font-size: 0.8125rem;
    line-height: 1.6;
  }

  .kui-callout__body p {
    margin: 0.375rem 0;
  }

  .kui-callout__body ul {
    margin: 0.375rem 0;
    padding-left: 1.25rem;
  }

  .kui-callout__body li {
    margin: 0.25rem 0;
  }

  .kui-callout__body code {
    background: color-mix(in srgb, var(--callout-color) 10%, transparent);
    padding: 0.0625rem 0.25rem;
    border-radius: 0.1875rem;
    font-size: 0.75rem;
  }
</style>
