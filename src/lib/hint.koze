<script>
  import { props } from 'koze:component';
  const { color = 'var(--kui-fg-muted)', class: className = '' } = props;
</script>

<p class="kui-hint {className}" style="--hint-color: {color}">
  <slot></slot>
</p>

<style>
  .kui-hint {
    margin: 0 0 0.5rem 0;
    padding: 0.375rem 0.625rem;
    border-radius: var(--kui-radius-md);
    background: color-mix(in srgb, var(--hint-color) 8%, transparent);
    border: 1px solid color-mix(in srgb, var(--hint-color) 20%, transparent);
    font-size: 0.75rem;
    color: var(--kui-fg);
  }
</style>
