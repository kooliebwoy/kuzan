<script>
  import { props } from 'koze:component';
  const { title, class: className = '' } = props;
</script>

<article class="kui-card {className}" data-compact={props.data_compact}>
  <div class="kui-card__body">
    if (title) {
      <h3 class="kui-card__title">{title}</h3>
    }
    <slot></slot>
  </div>
</article>
